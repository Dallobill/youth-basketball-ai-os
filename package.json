{
  "name": "youth-basketball-ai-os-starter",
  "version": "0.1.0",
  "description": "Starter backend for a youth basketball development operating system",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "lint": "eslint src test --ext .js",
    "format:check": "prettier --check \"src/**/*.js\" \"test/**/*.js\" \".github/workflows/*.yml\" \"*.json\"",
    "format": "prettier --write \"src/**/*.js\" \"test/**/*.js\" \".github/workflows/*.yml\" \"*.json\"",
    "test": "node --test test/**/*.test.js",
    "ci": "npm run lint && npm run format:check && npm run test && npm --prefix frontend run lint && npm --prefix frontend run format:check && npm --prefix frontend run test",
    "schema:check": "node --test test/backend/schema-contract.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
